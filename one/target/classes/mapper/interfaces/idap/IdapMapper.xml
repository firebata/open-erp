<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skysport.core.mapper.IdapMapper">

    <select id="findAllDeveloper" resultType="String">
        select
           user_id
        FROM t_qx_user a
        WHERE a.project_id =#{natrualkey}
        limit 1
    </select>

    <select id="findAllOPer" resultType="String">
        select
        user_id
        FROM t_qx_user a
        WHERE a.project_id =#{natrualkey}
        limit 1
    </select>

    <select id="findManagerOfDevDept" resultType="String">
        select
        user_id
        FROM t_qx_user a
        WHERE a.project_id =#{natrualkey}
        limit 1
    </select>

    <select id="findManagerOfOP" resultType="String">
        select
        user_id
        FROM t_qx_user a
        WHERE a.project_id =#{natrualkey}
        limit 1
    </select>

</mapper>